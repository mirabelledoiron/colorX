{
  "name": "colorx",
  "private": true,
  "version": "2.0.0",
  "description": "Generate WCAG and APCA-compliant light and dark themes from a single hex color.",
  "scripts": {
    "dev": "pnpm --filter @colorx/web dev",
    "build": "pnpm --filter @colorx/core build && pnpm --filter @colorx/web build",
    "test": "pnpm -r test",
    "lint": "eslint ."
  }
}
