{
  "buildCommand": "pnpm --filter @colorx/core build && pnpm --filter @colorx/web build",
  "outputDirectory": "apps/web/dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
